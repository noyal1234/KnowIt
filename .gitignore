.DS_Store
.venv/
*.log

# Environment / secrets
.env
.env.local
.env.*.local
*.pem
credentials.json
service-account*.json

# Cursor — local AI rules/skills (not shared via git for now)
.cursor/rules/
.cursor/skills/

# Xcode (monorepo — also see frontend/.gitignore)
**/xcuserdata/
**/*.xcuserstate
**/DerivedData/
**/build/
